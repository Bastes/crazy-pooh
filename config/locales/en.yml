en:
  helpers:
    submit:
      user_session:
        create: "Log in"
  activerecord:
    models:
      user: "user"
    attributes:
      user:
        login: "login"
        password: "password"
  activemodel:
    models:
      user_session: "session"
    attributes:
      user_session:
        login: "login"
        password: "password"
    errors:
      models:
        user_session:
          authentication: "These login and password doesn't match"
