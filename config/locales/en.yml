en:
  links:
    new: "new %{model}"
    delete: "delete"
  notices:
    created: "%{model} created"
    updated: "%{model} updated"
    deleted: "%{model} deleted"
  layouts:
    admin:
      header:
        login: "Log in"
        logged_as: "Logged as \"%{login}\""
        logout: "Log out"
  admin:
    user:
      edit:
        title: "Edit your user informations"
    user_session:
      show:
        title: "Administration"
    static_contents:
      index:
        title: "All static contents"
      edit:
        title: "Edit the static content for %{codename}"
    external_links:
      index:
        title: "All external links"
      new:
        title: "Create a new external link"
      edit:
        title: "Edit the external link"
  helpers:
    submit:
      create: "Create this %{model}"
      update: "Update this %{model}"
      user_session:
        create: "Log in"
  activerecord:
    models:
      user: "user"
      static_content: "static content"
      external_link: "external link"
    attributes:
      user:
        login: "login"
        password: "password"
      static_content:
        codename: "code name"
        content: "content"
      external_link:
        section: "section"
        rank: "rank"
        label: "label"
        description: "description"
        url: "url"
        banner: "banner"
  activemodel:
    models:
      user_session: "session"
    attributes:
      user_session:
        login: "login"
        password: "password"
    errors:
      models:
        user_session:
          authentication: "These login and password doesn't match"
  errors:
    messages:
      blank: "can't be blank"
      too_short: "is too short (min %{count} characters)"
      taken: "is already taken by another %{model}"
